# ReDroid WhatsApp

Place `WhatsApp.apk` in this app's Home Assistant add-on config folder and restart the app. Once ReDroid boots, the APK is installed automatically.

ADB is available on port 5555. Keep that port restricted to trusted networks.

This is an experimental app because ReDroid depends on host Binder/kernel functionality and elevated container privileges.

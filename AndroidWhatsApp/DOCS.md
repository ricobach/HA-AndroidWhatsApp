# AndroidWhatsApp

Place `WhatsApp.apk` in this app's Home Assistant add-on config folder and restart the app. Once Android boots, the APK is installed automatically.

ADB is available on port 5555. Keep that port restricted to trusted networks.

This is an experimental app because its ReDroid-based Android environment depends on host Binder/kernel functionality and elevated container privileges.
